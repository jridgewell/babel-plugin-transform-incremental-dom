{
  "name": "babel-plugin-incremental-dom",
  "version": "1.0.4",
  "description": "Turn jsx into incremental-dom",
  "repository": "thejameskyle/babel-plugin-incremental-dom",
  "license": "MIT",
  "main": "lib/index.js",
  "devDependencies": {
    "babel": "^5.6.0",
    "mocha": "^2.2.5"
  },
  "scripts": {
    "build": "babel-plugin build",
    "push": "babel-plugin publish",
    "test": "mocha --compilers js:babel/register"
  },
  "keywords": [
    "babel-plugin"
  ],
  "babel": {
      "loose": true
  }
}
